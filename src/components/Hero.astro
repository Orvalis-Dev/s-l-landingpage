<section class="hero" aria-label="Presentación principal">
  <div class="hero-overlay"></div>
  <div class="container hero-content">
    <div class="hero-copy">
      <span class="eyebrow">S&amp;L SERVICIOS INDUSTRIALES</span>
      <h1>
        Soluciones Industriales<br />
        Confiables Hace 20 Años
      </h1>
      <div class="hero-line" aria-hidden="true"></div>
      <p class="section-text hero-text">
        Mantenimiento mecánico y provisión de elementos de protección personal para empresas del Norte Argentino.
      </p>

      <div class="hero-actions">
        <a class="btn btn-primary" href="https://wa.me/5493810000000" target="_blank" rel="noreferrer">
          Solicitar Asesoramiento
        </a>
        <a class="btn btn-outline" href="#catalogo">Descargar Catálogo</a>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    isolation: isolate;
    min-height: 100vh;
    display: flex;
    align-items: center;
    background-image:
      linear-gradient(
        90deg,
        rgba(0, 0, 0, 0.92) 0%,
        rgba(0, 0, 0, 0.85) 35%,
        rgba(0, 0, 0, 0.55) 60%,
        rgba(0, 0, 0, 0.3) 100%
      ),
      url('https://images.unsplash.com/photo-1581092335397-9583eb92d232?auto=format&fit=crop&w=2000&q=80');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    padding: 0;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    z-index: -1;
    background:
      linear-gradient(120deg, rgba(9, 16, 26, 0.42) 0%, rgba(9, 16, 26, 0) 60%),
      radial-gradient(circle at 78% 30%, rgba(62, 92, 125, 0.18) 0%, rgba(62, 92, 125, 0) 45%);
    pointer-events: none;
  }

  .hero::after {
    content: '';
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    height: 7.5rem;
    background: linear-gradient(to bottom, rgba(30, 30, 30, 0) 0%, #1e1e1e 100%);
    pointer-events: none;
  }

  .hero-content {
    width: 100%;
    padding-top: 4.5rem;
    padding-bottom: 2rem;
  }

  .hero-copy {
    max-width: 44rem;
    animation: hero-fade-in 650ms ease-out;
  }

  .hero :global(.eyebrow) {
    margin-bottom: 0.9rem;
    letter-spacing: 0.14em;
  }

  .hero h1 {
    font-size: clamp(2.8rem, 5vw, 4.5rem);
    line-height: 1.05;
    max-width: 45rem;
    text-wrap: balance;
  }

  .hero-line {
    width: 3.75rem;
    height: 0.2rem;
    margin: 1.1rem 0 1.5rem;
    background-color: var(--color-primary);
  }

  .hero-text {
    max-width: 58ch;
    font-size: 1rem;
    color: #dddddd;
  }

  .hero-actions {
    margin-top: 1.9rem;
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
  }

  .hero-actions .btn {
    width: 100%;
    min-height: 3.35rem;
    font-size: 1.02rem;
    padding: 0.95rem 1.55rem;
    transition:
      transform 0.2s ease,
      background-color 0.2s ease,
      border-color 0.2s ease,
      color 0.2s ease;
  }

  .hero-actions .btn:hover {
    transform: scale(1.02);
  }

  .hero-actions .btn-outline {
    border-color: rgba(248, 249, 250, 0.82);
    background-color: rgba(17, 17, 17, 0.25);
  }

  @keyframes hero-fade-in {
    from {
      opacity: 0;
      transform: translateY(0.8rem);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (min-width: 768px) {
    .hero-content {
      padding-top: 5.25rem;
      padding-bottom: 3rem;
    }

    .hero-line {
      width: 3.9rem;
    }

    .hero-actions {
      flex-direction: row;
      align-items: center;
      gap: 1rem;
    }

    .hero-actions .btn {
      width: auto;
      min-width: 15.4rem;
    }
  }

  @media (min-width: 1024px) {
    .hero {
      background-position: center;
    }

    .hero-content {
      padding-top: 5.6rem;
    }

    .hero-copy {
      max-width: 45rem;
    }
  }

  @media (max-width: 767px) {
    .hero {
      background-position: 68% center;
    }

    .hero-content {
      text-align: center;
      padding-inline: 1.5rem;
      padding-top: 4.9rem;
      padding-bottom: 1.8rem;
    }

    .hero-copy {
      margin-inline: auto;
      max-width: 34rem;
    }

    .hero h1 {
      font-size: clamp(2rem, 8vw, 3rem);
      line-height: 1.1;
      max-width: 100%;
    }

    .hero-line {
      margin-inline: auto;
    }
  }
</style>
